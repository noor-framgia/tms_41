<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
	<%= link_to t(:framgia), root_path , id: "logo" %>
	<nav>
	  <ul class="nav navbar-nav navbar-right">
		<% if false %>
      <% if true %>
        <li><%= link_to t(:home),'#' %></li>
        <li><%= link_to t(:account),'#' %></li>
        <li><%= link_to t(:my_courses),'#' %></li>
			<% else %>
        <li><%= link_to t(:home),'#' %></li>
        <li><%= link_to t(:account),'#' %></li>
        <li><%= link_to t(:my_courses),'#' %></li>
      <% end %>
    <% else %>
      <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
          <div class="navbar-header">
            <%= link_to t(:framgia), root_path , id: "logo" %>
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
          </div>
          <div id="navbar" class="navbar-collapse collapse">
            <form class="navbar-form navbar-right">
          <% if user_signed_in? %>
              <div class="form-group">
                <%= link_to t(:supervisor_text_header), supervisor_users_path, class:"btn btn-default" %>
              </div>
              <div class="form-group">
                <%= link_to t(:trainee_text_header), trainee_users_path, class:"btn btn-default" %>
              </div>
              <div class="form-group">
                <%= link_to t(:sign_out_text), destroy_user_session_path, class:"btn btn-default" %>
              </div>
          <% else %>
              <div class="form-group">
                <%= link_to t(:sign_in_text), new_user_session_path, class:"btn btn-default"%>
              </div>
          <% end %>
            </form>
          </div><!--/.navbar-collapse -->
        </div>
      </nav>
    <% end %>
	  </ul>
	</nav>
  </div>
</header>